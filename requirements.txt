# RAIA — Responsible AI Assistant
# Python >= 3.10 recommended.

# UI
streamlit>=1.37

# Orchestration (state graph with native human-interruption points)
langgraph>=0.2.60
langchain-core>=0.3

# Reference LLM (paper Section 3.3). Swap/add providers freely:
langchain-anthropic>=0.3
# langchain-openai>=0.2   # uncomment to use OpenAI models

# RAG vector store
chromadb>=0.5.5
# Hosted platforms (Streamlit Community Cloud) ship an old system sqlite3;
# this provides a modern one, swapped in by raia/__init__.py. Linux-only.
pysqlite3-binary; sys_platform == "linux"

# Data handling (Drift Monitor fairness metrics)
pandas>=2.0

# Configuration
python-dotenv>=1.0
